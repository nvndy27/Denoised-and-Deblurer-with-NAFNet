import 'package:flutter/material.dart';

class WebCameraHelper {
  static Future<String?> captureImage(BuildContext context) async {
    throw UnimplementedError('WebCameraHelper is only supported on Web.');
  }
}
