class AppConstants {
  AppConstants._();

  static const String appName = 'Denoiser';
  static const String appSubtitle = 'Restore clear images using Deep Learning';
  static const Duration mockInferenceDelay = Duration(seconds: 2);
}
