# NAFNet weights

Đặt checkpoint `.pth` thật vào thư mục này:

- `nafnet_sidd_width32.pth`
- `nafnet_sidd_width64.pth`
- `nafnet_custom.pth`

Nếu chưa có checkpoint, backend vẫn chạy được khi `USE_MOCK_INFERENCE=true`.
